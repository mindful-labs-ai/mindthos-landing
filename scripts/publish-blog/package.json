{
  "name": "mindthos-publish-blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "마음토스 블로그 자동 발행 파이프라인 — SEO 분석 → 교차 검증 → 인링크 → 이미지 → DB 저장",
  "scripts": {
    "publish": "tsx src/publish.ts",
    "inlinks": "tsx src/insert-inlinks.ts",
    "enrich": "tsx src/enrich-existing.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.101.1",
    "dotenv": "^16.4.5",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  }
}
